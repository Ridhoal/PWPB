<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Daftar Barang</title>
</head>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
  integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"
  integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA=="
  crossorigin="anonymous" referrerpolicy="no-referrer" />

<body>
  <style>
    .btn-grad {
      background-image: linear-gradient(to right, #DA22FF 0%, #9733EE 51%, #DA22FF 100%);
      margin: 10px;
      /* padding: 15px 45px; */
      text-align: center;
      text-transform: uppercase;
      transition: 0.5s;
      background-size: 200% auto;
      color: white;
      box-shadow: 0 0 20px #eee;
      border-radius: 10px;
      display: block;
      width: 129px;
      height: 45px;
      font-size: small;
    }

    .btn-grad:hover {
      background-position: right center;
      /* change the direction of the change here */
      color: #fff;
      text-decoration: none;
    }
  </style>
  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Navbar</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="#">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Features</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Pricing</a>
          </li>
          <li class="nav-item">
            <a class="nav-link disabled" aria-disabled="true">Logout</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
  <a href="/shop/<%= idJ %>">
    <i class="fa-solid fa-shop fa-bounce fa-lg" style="color: #176B87; margin-left: 2%; margin-top:3%;"></i>
  </a>
  <div class="container mt-5">
    <div class="row">
      <!-- 1 -->
      <div class="col-md-6">
        <!-- Button trigger modal -->
        <% if (user) { %>
          <% if (user.saldo <a.total_harga) { %>
            <div style="color: red;">Saldo Ti</div>

            <button type="button" class="badge bg-primary data-bs-toggle=" modal" data-bs-target="#exampleModal"
              onclick="bayar('<%= user.saldo %>', '<%= a.total_harga %>', '<%= a.id_barang %>','<%= a.Nama_barang %>','<%= a.id_transaksi %>','<%= jumlah %>' ,'<%= stock %>')">
              bayar
            </button>
            <% } %>
              <% } %>


                <!-- Button trigger modal -->

                <table class="table table-dark table-striped mt-5">
                  <tr>
                    <th>id</th>
                    <th>Nama barang</th>
                    <th>harga</th>
                    <th>stock</th>
                    <!-- <th>new stock</th> -->
                    <th>pilih</th>
                    <th>action</th>
                  </tr>
                  <% for(let a of barangs ){ %>
                    <tr>
                      <td>
                        <%= a.id_barang %>
                      </td>
                      <td>
                        <%= a.Nama_barang %>
                      </td>
                      <td>
                        <%= formatsaldo(a.harga) %>
                      </td>
                      <td>
                        <%= a.new_stock %>
                      </td>
                      <td>
                        <% if (a.new_stock <=0) {%>
                          <div style="color: red;">stock habis</div>
                          <% }else{ %>
                            <button type="button" class="badge bg-success" data-bs-toggle="modal"
                              data-bs-target="#modalBarang"
                              onclick="barang('<%= a.id_barang %>', '<%= a.harga %>', '<%= a.new_stock %>')">
                              Pilih
                            </button>
                            <% } %>
                      </td>
                      <td>
                        <a href="/hapusBarang/<%= a.id_barang %>" class="badge bg-warning"
                          onclick="return confirm ('Yakin ingin menghapus?')">Hapus</a>
                        <button type="button" class="badge bg-danger" data-bs-toggle="modal" data-bs-target="#modalEdit"
                          onclick="edit('<%= a.id_barang %>', '<%= a.Nama_barang%>', '<%= a.harga%>')">
                          Edit
                        </button>

                      </td>
                    </tr>

                    <% } %>

                </table>

      </div>

      <!-- 2 -->
      <%- include("modal.ejs") %>

    </div>
    



    <script src="/script.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz"
      crossorigin="anonymous"></script>
  </div>
  </div>
</body>


</html>